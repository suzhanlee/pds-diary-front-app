import { View, Text } from "react-native-web";
import styles from "./styles";

export default function DaySee() {
    return (
        <View style={styles.day_container}>
            <View style={styles.headerText}>SEE</View>
            <Text>day see</Text>
        </View>
    );
}
