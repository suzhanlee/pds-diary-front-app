import { StyleSheet } from "react-native";

export default StyleSheet.create({
    day_container: {
        flex: 0.5,
        minHeight: 100,
        backgroundColor: "pink",
        padding: 10,
    },
    headerText: {
        fontSize: 18,
        fontWeight: "bold",
        marginBottom: 10,
        textAlign: "center",
    },
});
